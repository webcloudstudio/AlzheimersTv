{
  "name": "alzheimerstv-downloader",
  "version": "1.0.0",
  "description": "Streaming show catalog pipeline for AlzheimersTv — fetches metadata and streaming URLs",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "init": "tsc && node dist/scheduler.js init",
    "bulk": "tsc && node dist/scheduler.js bulk",
    "seed": "tsc && node dist/scheduler.js seed",
    "pipeline:full": "tsc && node dist/scheduler.js pipeline:full",
    "pipeline": "tsc && node dist/scheduler.js pipeline",
    "pipeline:verify": "tsc && node dist/scheduler.js pipeline:verify",
    "generate": "tsc && node dist/scheduler.js generate"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "csv-parse": "^5.5.6",
    "streaming-availability": "^4.4.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
